spring.application.name=jobportal
#to fix the changing jdbc url every single time to a single url
#spring.datasource.url =  jdbc:h2:mem:currentdb
#to save the changes in the db and not reset the db to level zero
#allow multiple connections to the db AUTO_SERVER=true

spring.datasource.url=jdbc:h2:file:C:/Users/2742718/currentdb
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driverClassName=org.h2.Driver

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update

spring.sql.init.mode=always

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console